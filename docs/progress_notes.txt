=== Here the progress, notes and such will be documented ===


~Start of work 15:15 15/03/2026




When I call psutil.cpu_percent, I get a list containing 20 decimal numbers. That's because I am using a I5-14500T which has 20 processors (as seen in the task manager).
I will have to find a reasonable way to display them all, since I can't afford to "lose" data but I can't put it all in as well.
After a quick google search turns out the way window's task manager handles it is just an average. I'll do the same.


Getting the results was very stright forward, might add graph in a bit to make it more user friendly. נראה..

I noticed I get results from the Disk and RAM readings that are slightly different than the ones I see in the task manager, I looked it up and it turns out windows is calculating GBs as 1024**3 
while most people (myself included) do it as 10^9. 

I had to figure out how to create the Disk table, since it's a bit different measurements than CPU and RAM in my opinion, I sattled on creating a dict in getDisk, that has the partition letter, and the disk object
created by psuitl. I had to figure out how to deal with all the data (combination of dict, lists, objects and more) I tried to make my code as readable as I can..


Also, while working on the logger module I figured out logging all cores in a csv file would be rather "interesting". Should I make *amount of cores* coloums or one column with all cores inside, I sattled on the second one. 
Later I've faced the same issue with the disk stats. A user could have 20 partitions. I thought the best way is to create a "Starting point" which will add all the partition at time of program start then if no info 
is available I'll put in "N/A" aka not accessable.